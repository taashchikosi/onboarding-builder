# Copy this file to ".env" and paste your real values after the = signs.
# .env is gitignored — it never gets committed and never goes into chat.

# Step 1 — DeepSeek (the compile LLM)
OAB_DEEPSEEK_KEY=

# Step 2 — HubSpot SANDBOX private-app token (scoped, least-privilege)
OAB_HUBSPOT_TOKEN=
